// Traversing array for loop
var arr = ["selva", "vjselva", "moni"];
for (var i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}
// using map
console.log("using map");
arr.map(function (item) {
    console.log(item);
});
