// Traversing array for loop
let arr:string[]=["selva","vjselva","moni"];
for(let i=0;i<arr.length;i++){
    console.log(arr[i]);
}

//  map
console.log("using map");
arr.map((item)=>{
console.log(item);
})
